@model IEnumerable<BloomBox.Models.Lokacija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.lokacijaKorisnikaURL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.lokacijaURL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lokacijaKorisnikaURL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.lokacijaURL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.lokacijaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.lokacijaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.lokacijaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
