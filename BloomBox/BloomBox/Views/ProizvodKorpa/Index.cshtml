@model IEnumerable<BloomBox.Models.ProizvodKorpa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.proizvodId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.narudzbaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kolicina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.proizvodId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.narudzbaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kolicina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.proizvodKorpaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.proizvodKorpaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.proizvodKorpaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
