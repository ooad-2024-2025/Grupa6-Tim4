@model IEnumerable<BloomBox.Models.ValidacijaProizvoda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.stanjeProizvoda)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.stanjeProizvoda)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.validacijaProizvodaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.validacijaProizvodaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.validacijaProizvodaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
