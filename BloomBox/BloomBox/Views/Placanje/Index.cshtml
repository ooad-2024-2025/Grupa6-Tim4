@model IEnumerable<BloomBox.Models.Placanje>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.narudzbaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.transakcijskiId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.narudzbaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.transakcijskiId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.placanjeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.placanjeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.placanjeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
