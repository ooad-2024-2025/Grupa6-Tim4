@model IEnumerable<BloomBox.Models.AdresaDostave>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.grad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.postanskiBroj)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.grad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postanskiBroj)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.adresaDostaveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.adresaDostaveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.adresaDostaveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
